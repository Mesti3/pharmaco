<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:pharmaco.components.menu"
             xmlns:button="clr-namespace:pharmaco.components.button" x:Class="pharmaco.components.menu.button_menu"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="150">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <button:green_button x:Name="button" Grid.Row="0" GreenButtonText="Kategórie" HorizontalAlignment="Stretch" Margin="0,0,0,0" VerticalAlignment="Top" Height="100" Click="button_Click" />
        <Canvas Grid.Row="1" MouseLeave="Canvas_MouseLeave" >
            <local:green_menu x:Name="menu" HorizontalAlignment="Left" Margin="0,0,0,0" Grid.Row="1" VerticalAlignment="Top" Height="auto" Width="auto" item_selected="menu_item_selected" visibility_changed="menu_visibility_changed"/>

        </Canvas>
         </Grid>
</UserControl>
